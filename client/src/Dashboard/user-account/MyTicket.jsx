import useFetchData from "../../hooks/useFetchData";
import { BASE_URL } from "../../config";

const MyTicket = () => {
  return (
    <section>
      <h2 className="text-textColor">Will be displayed soon....</h2>
    </section>
  );
};

export default MyTicket;
